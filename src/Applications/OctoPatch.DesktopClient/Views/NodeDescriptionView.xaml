<UserControl x:Class="OctoPatch.DesktopClient.Views.NodeDescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OctoPatch.DesktopClient.Views"
             xmlns:octoPatch="clr-namespace:OctoPatch;assembly=OctoPatch"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance octoPatch:NodeDescription}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <Label Grid.Column="0" Grid.Row="0" Content="Name" />
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Name}" />

        <!-- Description -->
        <Label Grid.Column="0" Grid.Row="1" Content="Description" />
        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Description}" />

    </Grid>
</UserControl>
