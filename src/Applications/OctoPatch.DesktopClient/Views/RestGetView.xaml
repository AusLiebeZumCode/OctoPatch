<UserControl x:Class="OctoPatch.DesktopClient.Views.RestGetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:OctoPatch.DesktopClient.Models"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance models:RestGetModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Uri" FontWeight="Bold" />
        <TextBox Grid.Row="1" Text="{Binding Uri}" Margin="5 0 5 10" />

    </Grid>
</UserControl>