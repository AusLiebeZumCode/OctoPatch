<UserControl x:Class="OctoPatch.DesktopClient.Views.KeyboardStringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:OctoPatch.DesktopClient.Models"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance models:KeyboardStringModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <CheckBox  IsChecked="{Binding IgnoreNotPrintable}">
            <TextBlock Text="Ignore not printable character like whitespace and newline" TextWrapping="Wrap"/>
        </CheckBox>

    </Grid>
</UserControl>